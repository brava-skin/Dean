# Core Dependencies
facebook-business==19.0.1
python-dotenv==1.0.1
PyYAML==6.0.2
requests==2.32.3
pytz>=2020.1,<2024
pandas==2.2.2
openpyxl==3.1.5
SQLAlchemy==2.0.35
prometheus-client==0.20.0
jsonschema==4.23.0
supabase>=2.5.0
schedule==1.2.0

# Data Processing (Required)
numpy>=1.24.0

# Creative Intelligence System (Required)
openai>=1.0.0  # GPT integration for creative prompts

# Creative Intelligence System (Optional - for semantic similarity)
sentence-transformers>=2.2.0  # Optional: Semantic similarity analysis

# Infrastructure (Optional - graceful degradation if missing)
redis>=5.0.0  # Optional: Redis caching (falls back to in-memory)
psutil>=5.9.0  # Optional: Resource monitoring (falls back gracefully)
Pillow>=10.0.0  # Optional: Image optimization (falls back gracefully)

# Note: ffmpeg is required as a system binary (not a Python package)
# Install with: brew install ffmpeg (macOS) or apt-get install ffmpeg (Linux)
# The code uses subprocess to call ffmpeg directly, not ffmpeg-python
